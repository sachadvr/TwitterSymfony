<div class="beforeCommentaire borderCommentaire border border-black p-3 flex flex-row gap-3 relative" onclick="document.querySelector('.commentaire_area').focus(); document.querySelector('.commentaire_area').value =  document.querySelector('.commentaire_area').value + '@{{ commentaire.createdBy.username }} ';">
    <img src="{{ asset(commentaire.createdBy.getImagePath())}}" alt="image" class="h-10 w-10 object-contain rounded-full flex-grow-0">
    <div class="flex-1">
    <h2 class="text-sm">@{{  commentaire.createdBy.username }} 
    {% include '/components/_following.html.twig' with {'createdBy': commentaire.createdBy} %}
    </h2>
    <p>{{ this.replacedContent(commentaire.contenu) | raw}}</p>
    </div>
</div>